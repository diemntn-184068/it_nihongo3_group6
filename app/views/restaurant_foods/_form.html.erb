<div class="form-add-infomation">
    <%= form_with(model: restaurant_food, local: true) do |form| %>
  <% if restaurant_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant_food.errors.count, "error") %> prohibited this restaurant_food from being saved:</h2>

      <ul>
        <% restaurant_food.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field ">
    <%= form.label :restaurant_id %>
    <%= form.number_field :restaurant_id, class:"input-data-form" %>
  </div>

  <div class="field">
    <%= form.label :name, "Food" %>
    <%= form.text_field :name, class:"input-data-form" %>
  </div>

  <div class="field">
    <%= form.label :description, "Review Content" %>
    <%= form.text_area :description, class:"input-data-form" %>
  </div>

  <div class="field">
     <%= form.label :image %>
     <%= form.file_field :image, class:"image-upload" %><br>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</div>